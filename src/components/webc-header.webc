<header class="header" webc:root="override">
  <slot></slot>
</header>

<style>
  .header {
    padding-block: 1rem;
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    align-items: center;
    gap: 1em;
  }
</style>